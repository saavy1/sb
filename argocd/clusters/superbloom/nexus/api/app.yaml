apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nexus-api
  namespace: argocd
  annotations:
    kargo.akuity.io/authorized-stage: nexus:prod
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: nexus
  sources:
    - repoURL: https://bjw-s-labs.github.io/helm-charts
      chart: app-template
      targetRevision: "4.1.1"
      helm:
        releaseName: nexus
        valueFiles:
          - $values/argocd/clusters/superbloom/nexus/api/values.yaml
    - repoURL: https://github.com/saavy1/sb.git
      targetRevision: main
      ref: values
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
      - ServerSideApply=true
