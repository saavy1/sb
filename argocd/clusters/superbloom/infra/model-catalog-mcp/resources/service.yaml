apiVersion: v1
kind: Service
metadata:
  name: model-catalog-mcp
  namespace: hermes
  labels:
    app.kubernetes.io/name: model-catalog-mcp
    app.kubernetes.io/part-of: hermes
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: model-catalog-mcp
  ports:
    - name: http
      port: 8080
      targetPort: http
