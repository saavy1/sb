apiVersion: v1
kind: Service
metadata:
  name: github-mcp-server
  namespace: hermes
  labels:
    app.kubernetes.io/name: github-mcp-server
    app.kubernetes.io/part-of: hermes
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: github-mcp-server
  ports:
    - name: http
      port: 8082
      targetPort: proxy
