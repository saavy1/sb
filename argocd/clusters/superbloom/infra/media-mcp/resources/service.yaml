apiVersion: v1
kind: Service
metadata:
  name: media-mcp
  namespace: hermes
  labels:
    app.kubernetes.io/name: media-mcp
    app.kubernetes.io/part-of: hermes
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: media-mcp
  ports:
    - name: http
      port: 8080
      targetPort: http
