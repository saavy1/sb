apiVersion: v1
kind: Service
metadata:
  name: buildkit
  namespace: buildkit
spec:
  type: ClusterIP
  selector:
    app: buildkit
  ports:
    - port: 1234
      targetPort: 1234
      protocol: TCP
