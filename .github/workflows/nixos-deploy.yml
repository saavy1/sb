name: NixOS Deploy

on:
  push:
    branches: [main]
    paths:
      - "nixos/**"
  workflow_dispatch:
    inputs:
      force:
        description: "Force rebuild even without changes"
        required: false
        default: "false"

jobs:
  trigger-rebuild:
    name: Trigger NixOS Rebuild
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Placeholder
        run: echo "NixOS deploy pipeline retained; webhook to removed Nexus service deleted"
